import { MessageBoxPositionDirective } from './message-box-position.directive';

describe('MessageBoxPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageBoxPositionDirective();
    expect(directive).toBeTruthy();
  });
});
